import { createContext, useContext } from '@wordpress/element';

import { QueryRule } from './types';

type RuleProps = { rule: QueryRule };

const Rule = ( { rule }: RuleProps ): JSX.Element => {
	return <></>;
};

export default Rule;
